# AGENTS.md

## 工具使用策略

### 无需审批的命令（只读/安全操作）

以下命令可以直接执行，无需用户确认：

#### 网络请求（只读）
- `curl` - GET请求、HEAD请求
- `wget` - 下载文件
- HTTP状态码检查
- API端点验证

#### 文件操作（只读）
- `ls`, `dir` - 列出目录
- `cat`, `head`, `tail` - 查看文件内容
- `grep`, `find` - 搜索文件和内容
- `wc`, `stat` - 文件统计信息
- `read` - 读取文件

#### 进程检查
- `ps` - 查看进程
- `top`, `htop` - 系统资源监控
- `lsof` - 查看打开的文件

#### 网络检查
- `ping` - 网络连通性测试
- `netstat`, `ss` - 网络连接状态
- `dig`, `nslookup` - DNS查询

#### 开发工具
- `git status`, `git log`, `git diff` - Git只读操作
- `npm run dev`, `yarn dev` - 启动开发服务器
- `python3 -m http.server` - 启动HTTP服务器
- `uvicorn`, `fastapi` - 启动后端服务

### 需要审批的命令（写操作/危险操作）

以下命令执行前需要用户确认：

#### 文件修改
- 创建、编辑、删除文件
- `rm`, `mv`, `cp` - 文件操作
- `chmod`, `chown` - 权限修改

#### 系统操作
- `sudo` - 提权操作
- `apt`, `yum`, `brew` - 包管理
- `pip install`, `npm install` - 依赖安装

#### 危险操作
- 任何删除操作
- 数据库写操作
- 网络端口开放
- 环境变量修改

## 本项目特殊规则

### IP-Creator项目

1. **服务重启**: 可以自动重启开发服务器（uvicorn, vite）
2. **API测试**: 可以直接测试本地API端点
3. **数据库查询**: 只读查询无需审批
4. **日志查看**: 可以随时查看服务日志

### 默认行为

- curl命令默认无需审批（仅用于API测试和验证）
- 本地服务（localhost）操作默认安全
- 只读操作自动执行
- 写操作需要明确确认

## 智能判断

当遇到以下情况时，即使是一般安全的命令也需要确认：

1. 第一次执行某种命令
2. 命令参数包含危险选项（如 `rm -rf`）
3. 涉及生产环境
4. 可能影响系统稳定性

## 用户偏好

用户倾向于：
- 快速迭代开发
- 自动化常规检查
- 减少不必要的确认步骤
- 但仍需要关键操作的透明度
